import { Meta, Story } from '@storybook/blocks';
import * as GridStories from '../../../stories/grid/grid.stories';

<Meta title="Docs/Developers/Mixins/Grid/grid-col-end"/>

# grid-col-end

The `grid-col-end` mixin can be used to specifies a grid items end position within the grid.

```css
@mixin grid-col-end {position};
```

<div className="table margin-bottom">
    <div>
        <div>Parameter</div>
        <div>Options</div>
    </div>
    <div>
        <div>`position`</div>
        <div>The items end position, a number value or `auto`</div>
    </div>
</div>

### Responsive Grid Column End

The `grid-col-end` mixin can be adjusted by breakpoint using the responsive version.

```css
@mixin grid-{breakpoint}-col-end {position};
```

<div className="table margin-bottom">
    <div>
        <div>Parameter</div>
        <div>Options</div>
    </div>
    <div>
        <div>`breakpoint`</div>
        <div>`sm` `md` `lg` `xl` `xxl`</div>
    </div>
    <div>
        <div>`position`</div>
        <div>The items end position, a number value or `auto`</div>
    </div>
</div>