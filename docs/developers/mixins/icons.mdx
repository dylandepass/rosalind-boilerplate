import { Meta, Story } from '@storybook/blocks';
import theme from '../../../styles/themes/rosalind-light.json';
import { Code } from '../../stories/util';

<Meta title="Docs/Developers/Mixins/Icons"/>

# Icon Mixins

Rosalind provides the following mixin to work with icons.

```css
@mixin icon {size};
```

<div className="table margin-top">
    <div>
        <div>Parameter</div>
        <div>Options</div>
    </div>
    <div>
        <div>`size`</div>
        <div>{ Object.entries(theme.semantic.size.icon).map(([k, v]) => <Code k={k} value={v} url="/?path=/docs/docs-developers-tokens-icon--docs"/>) }</div>
    </div>
</div>